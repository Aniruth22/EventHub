import React from 'react';
import { Container, Typography } from '@mui/material';

const SettingsPage = () => {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>Settings</Typography>
      <Typography sx={{ mt: 2 }}>This is a placeholder for the settings page.</Typography>
    </Container>
  );
};
export default SettingsPage;